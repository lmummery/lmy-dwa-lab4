<!doctype html>
<html>
  <head>
    <title><%=forumName%> - Users</title>
    <link rel="stylesheet" type="text/css" href="main.css" />
  </head>
  <body>
    <h1><%=forumName%></h1>
    <h2>Users</h2>
    <table>
    <tr><th>Username</th><th>First Name</th><th>Surname</th></tr>
    <% users.forEach(function(user) { %>
      <tr>
        <td><%= user.username %></td>
        <td><%= user.firstname %></td>
        <td><%= user.surname %></td>
    </tr>
    <% }) %>      
    </table>  
  </body>
</html>